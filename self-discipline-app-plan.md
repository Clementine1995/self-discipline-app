# 自律打卡 App 项目启动文档

## 1. 项目目标

做一个主要在安卓手机上使用的自律软件，帮助用户把每天想坚持的事情变成可提醒、可打卡、可统计、可奖励/惩罚的日常系统。

第一版先做本地 App，不急着接后端。核心目标是快速跑通：

- 每天定时提醒
- 到点完成后打卡
- 连续打卡获得奖励
- 失败达到次数触发惩罚
- 查看简单统计

## 2. 推荐技术方案

基于当前技术栈，推荐：

- 前端框架：Vue 3 + Vite
- 移动端 UI：Ionic Vue
- 打包为安卓 App：Capacitor
- 状态管理：Pinia
- 本地数据：第一版用 Capacitor Preferences 或 localStorage
- 本地提醒：Capacitor Local Notifications
- 后端：第一版不做，第二阶段再接 Spring Boot

这个方案的好处是可以继续使用 Vue，不需要从零学习 Flutter/Dart，也能比较快做出安卓可用版本。

## 3. MVP 功能范围

第一版只做最小可用产品。

### 3.1 习惯/任务管理

用户可以创建每天要做的任务，例如：

- 睡觉
- 运动
- 看书
- 早起
- 冥想

每个任务包含：

- 任务名称
- 提醒时间
- 是否启用提醒
- 每周执行日，第一版可先默认每天
- 奖励规则
- 惩罚规则

### 3.2 定时提醒

每天到指定时间，在手机上弹出本地通知。

第一版只需要支持：

- 每日固定时间提醒
- 点击通知后进入 App
- App 内显示今天待完成任务

### 3.3 每日打卡

用户可以在当天对任务进行打卡。

规则建议：

- 每个任务每天只能打卡一次
- 打卡时间记录下来
- 如果当天已完成，显示已打卡状态
- 第二天自动进入新一天状态

### 3.4 连续打卡奖励

系统根据连续完成天数计算 streak。

示例规则：

- 连续 3 天：小奖励
- 连续 7 天：中奖励
- 连续 30 天：大奖励

第一版奖励可以先只是文案提示，例如：

- “连续 7 天完成看书，可以奖励自己一杯咖啡”
- “连续 30 天运动，可以买一件喜欢的小东西”

### 3.5 失败惩罚

如果当天未完成任务，则记一次失败。

示例规则：

- 失败 1 次：轻提醒
- 连续失败 3 次：惩罚提示
- 一周失败超过 3 次：触发强惩罚

第一版惩罚也先做成文案提示，例如：

- “今晚少刷 30 分钟短视频”
- “明天补 20 分钟运动”
- “本周不能点奶茶”

### 3.6 统计页面

第一版统计不需要复杂图表，先做基础信息：

- 今日完成数量
- 今日未完成数量
- 每个任务的连续打卡天数
- 本周完成率
- 总失败次数

## 4. 页面规划

### 4.1 今日页

App 首页。

展示：

- 今天日期
- 今日任务列表
- 每个任务的提醒时间
- 完成/未完成状态
- 打卡按钮
- 今日完成进度

### 4.2 任务管理页

展示所有自律任务。

功能：

- 新建任务
- 编辑任务
- 删除任务
- 启用/停用提醒

### 4.3 新建/编辑任务页

字段：

- 名称
- 提醒时间
- 是否每天重复
- 奖励说明
- 惩罚说明
- 失败阈值

第一版可以不做复杂频率，只做“每天”。

### 4.4 统计页

展示：

- 本周完成率
- 连续打卡记录
- 失败次数
- 最近 7 天趋势

### 4.5 奖惩页

展示：

- 当前已解锁奖励
- 当前触发的惩罚
- 每个任务的奖惩规则

第一版可以先合并到任务详情页，后面再独立出来。

## 5. 数据结构草案

### 5.1 Habit

```ts
type Habit = {
  id: string;
  name: string;
  reminderTime: string; // HH:mm
  reminderEnabled: boolean;
  rewardText: string;
  punishmentText: string;
  failureThreshold: number;
  createdAt: string;
  updatedAt: string;
};
```

### 5.2 CheckIn

```ts
type CheckIn = {
  id: string;
  habitId: string;
  date: string; // YYYY-MM-DD
  checkedAt: string;
};
```

### 5.3 HabitStats

```ts
type HabitStats = {
  habitId: string;
  currentStreak: number;
  longestStreak: number;
  totalCheckIns: number;
  totalFailures: number;
};
```

## 6. 开发步骤

### 第一步：创建项目

创建 Vue + Ionic + Capacitor 项目。

目标：

- 项目能启动
- 手机端 UI 框架可用
- 可以构建 Android 项目

### 第二步：搭基础页面

先做页面骨架：

- 今日页
- 任务管理页
- 新建/编辑任务页
- 统计页

先不用追求美观，重点是路由和页面结构跑通。

### 第三步：实现任务 CRUD

实现：

- 新建任务
- 编辑任务
- 删除任务
- 查看任务列表
- 本地保存任务

### 第四步：实现每日打卡

实现：

- 今日任务列表
- 打卡按钮
- 防止重复打卡
- 按日期保存打卡记录
- 次日自动刷新状态

### 第五步：实现连续打卡计算

实现：

- 当前连续天数
- 最长连续天数
- 总打卡次数
- 未完成次数

### 第六步：实现奖励和惩罚提示

实现：

- 达到连续天数时展示奖励
- 失败达到阈值时展示惩罚
- 今日页或任务详情页展示当前状态

### 第七步：接入本地通知

实现：

- 请求通知权限
- 根据任务提醒时间创建通知
- 编辑任务时更新通知
- 删除任务时取消通知

### 第八步：安卓真机测试

重点测试：

- App 是否能安装
- 本地通知是否弹出
- 打卡记录是否持久保存
- 第二天日期切换是否正常
- 权限提示是否正常

## 7. 后续扩展方向

第二阶段可以考虑：

- 接 Spring Boot 后端
- 用户账号
- 云端同步
- 多设备同步
- 更复杂的重复规则，比如工作日、每周几
- 积分系统
- 奖励商店
- 惩罚保证金
- 好友监督
- 数据图表
- AI 复盘建议

## 8. AI 功能方向

后续可以把 AI 做成提高坚持率的辅助能力，而不是一开始就做复杂的 AI 教练。

优先考虑以下功能：

- AI 每日复盘：根据当天完成/失败情况，生成简短复盘和明天建议。
- AI 任务计划生成：用户输入长期目标，AI 自动拆成每天可执行的习惯任务。
- AI 失败后降级建议：当用户快要放弃时，把任务降级成最低可接受版本，例如“运动 30 分钟”降级为“拉伸 3 分钟”。
- AI 奖惩建议：根据任务难度和用户偏好，生成健康、可执行的奖励和惩罚。
- AI 智能调整提醒时间：根据用户历史打卡时间，建议更合适的提醒时间。
- AI 失败原因分析：每周总结失败集中出现的时间、任务类型和原因。
- AI 周报：生成本周完成率、最稳定习惯、最容易失败习惯和下周建议。

第一批最推荐实现：

1. AI 每日复盘
2. AI 任务计划生成
3. AI 失败后降级建议

## 9. 文案风格与语气系统

后续可以加入“文案人格/语气风格”功能，让提醒、打卡奖励、失败惩罚的文案可以按用户偏好变化。

可配置场景：

- 每日提醒语气
- 完成任务后的奖励语气
- 未完成任务后的惩罚语气
- 连续打卡里程碑语气
- 连续失败后的挽回/降级建议语气

可选风格示例：

- 温柔鼓励型：适合长期陪伴，语气轻柔、稳定。
- 严厉教练型：适合需要压力感的用户，语气直接、有督促感。
- 毒舌吐槽型：适合喜欢被调侃的用户，语气尖锐但不伤害。
- 傲娇挑衅型：类似“杂鱼杂鱼”式的挑衅感，但避免未成年化或露骨性暗示。
- 成人调教型：可借鉴 BDSM 的权力感、命令感和惩罚感，但必须基于用户主动选择，默认关闭，并提供尺度调节。
- 冷静理性型：像数据分析师，只给事实、原因和下一步建议。

尺度设置建议：

- 轻度：轻微吐槽、轻压力。
- 中度：明显挑衅、强督促、带一点羞耻感。
- 高度：更强命令感和惩罚感，仅限成人用户主动开启。

产品边界：

- 默认使用温和文案，不默认开启刺激型语气。
- 刺激型文案需要用户主动选择。
- 提供随时关闭和降低尺度的入口。
- 不做自伤、极端羞辱、歧视、威胁、人身攻击类文案。
- 不使用未成年化、儿童化或疑似未成年性化设定。
- 所有惩罚应保持现实可执行且安全，例如减少娱乐时间、补做低强度任务、写复盘，而不是伤害身体或制造危险。

## 10. 界面风格主题系统

界面风格也可以和文案语气一样做成可配置系统。它不只是换主题色，而是根据用户选择的“自律人格”同步调整视觉氛围、动效强度、反馈方式和组件形态。

核心思路：

- 文案语气决定“怎么说”。
- 界面主题决定“看起来像谁在陪你坚持”。
- 两者可以绑定，也可以分别设置。

可配置内容：

- 主色和辅助色
- 背景风格
- 卡片和按钮形态
- 打卡成功动画
- 失败惩罚弹窗样式
- 连续打卡里程碑视觉反馈
- 提醒页和统计页的视觉密度
- 图标风格
- 字体大小和信息层级

主题示例：

- 清爽日程型：浅色背景、蓝绿主色、卡片清晰，适合普通用户长期使用。
- 健身教练型：高对比色、强进度条、硬朗按钮，强调目标和执行力。
- 游戏成就型：等级、徽章、经验值、连击特效，适合奖励驱动。
- 暗黑纪律型：深色背景、红色/荧光强调、命令式弹窗，适合强约束感。
- 傲娇挑衅型：高饱和配色、夸张反馈、小剧场式弹窗，和挑衅文案搭配。
- 成人调教型：深色、低饱和、高对比、克制但有压迫感，适合命令感和惩罚感文案。
- 极简数据型：灰白背景、图表优先、少装饰，适合只想看结果的人。

实现建议：

- 第一版先做 2-3 个主题，不要一开始做太多。
- 主题系统使用 CSS Variables 管理，例如颜色、圆角、阴影、间距、动效速度。
- 文案风格和界面主题分开存储，方便用户混搭。
- 每个主题都要保证可读性，尤其是深色主题和高对比主题。
- 刺激型主题默认关闭，需要用户主动选择。
- 高刺激主题要提供“降低强度”入口，避免用户长期使用产生压力。

第一版推荐主题：

1. 清爽日程型
2. 游戏成就型
3. 暗黑纪律型

后续再扩展：

- 傲娇挑衅型
- 成人调教型
- 极简数据型

## 11. 扩展性设计原则

第一版虽然只做本地 MVP，但代码结构要为后续功能预留空间，避免后面接 AI、后端同步、主题系统和复杂奖惩规则时推倒重来。

核心原则：

- 业务逻辑和页面 UI 分开：页面只负责展示和交互，习惯、打卡、统计、奖惩计算放到独立 service/composable。
- 数据访问单独封装：不要在页面里直接读写 localStorage，统一通过 repository/storage 层访问，后续才能平滑替换成 SQLite 或 Spring Boot API。
- 规则系统可配置：奖励、惩罚、连续打卡、失败判定不要写死在按钮事件里，要抽成独立规则函数。
- 文案和主题独立：文案风格、提醒语气、界面主题不要散落在组件里，要集中配置。
- AI 能力作为插件式模块：AI 复盘、任务生成、失败降级建议都作为独立服务接入，不影响基础打卡功能。
- 本地优先：即使后续接后端，也保留本地缓存和离线可用能力。
- 类型先行：Habit、CheckIn、RewardRule、PunishmentRule、ThemeConfig、ToneProfile 等核心类型先定义清楚。
- 页面可渐进扩展：第一版可以简单，但路由和模块命名要为后续任务详情、周报、AI 教练、主题设置预留位置。

建议模块拆分：

- habits：任务创建、编辑、删除、启停用。
- checkins：每日打卡、取消打卡、防重复打卡。
- stats：连续天数、完成率、失败次数、趋势统计。
- reminders：本地通知权限、创建通知、更新通知、取消通知。
- rewards：奖励规则、里程碑判断、奖励提示。
- punishments：失败判断、惩罚规则、惩罚提示。
- ai：AI 复盘、任务生成、失败降级建议、周报。
- themes：界面主题配置、CSS Variables、主题切换。
- tones：文案人格、提醒语气、奖惩文案生成。
- storage：本地数据读写，后续可切换 SQLite 或远程 API。
- sync：后续账号、云同步、多设备同步。

建议目录结构：

```text
src/
  modules/
    habits/
    checkins/
    stats/
    reminders/
    rewards/
    punishments/
    ai/
    themes/
    tones/
    storage/
    sync/
  pages/
  components/
  router/
  stores/
  types/
  utils/
```

第一版不要把所有模块都实现完，但可以先按这个结构放置核心代码。这样后续加功能时会比较自然。

## 12. 新项目会话建议 Prompt

可以在新项目会话里这样开始：

```text
我要开发一个安卓为主的自律打卡 App。技术方案先用 Vue 3 + Ionic Vue + Capacitor，第一版不接后端，只做本地版。

核心功能：
1. 用户可以创建每天要做的任务，比如睡觉、运动、看书。
2. 每个任务可以设置每日定时提醒。
3. 用户每天可以对任务打卡。
4. 连续打卡有奖励提示。
5. 失败达到一定次数有惩罚提示。
6. 有一个简单统计页。

请先帮我搭建项目结构，并按 MVP 分阶段实现。优先保证安卓真机可运行。
```

## 13. 当前推荐决策

当前最推荐路线：

先用 Vue 3 + Ionic Vue + Capacitor 做本地安卓 MVP。

不建议第一版就做 Spring Boot 后端。先让 App 在手机上真正可用，验证自己是否愿意每天使用，再决定是否加入账号、同步、社交和更复杂的规则。
